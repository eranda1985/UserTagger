CREATE TABLE [dbo].[ServiceStatusSystems] (
    [Id]         VARCHAR (20)  NOT NULL,
    [Title]      VARCHAR (100) NOT NULL,
    [ActiveFlag] VARCHAR (1)   NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

